# Agent 开发执行记录

> **历史证据。** 本文保留 2026-09-08 的执行过程、当时的本机状态与真实 smoke 记录，不是当前 GitHub、部署或 G3/G4 状态页。当前核对结论见[项目状态入口](./project-state.md)；COL-23/COL-24 的实时状态以 Linear 为准。

2026-09-08 按 `agent-development-plan.md` v2 执行。G0、G1、G2 已通过；真实模型质量 G3 与七天使用效果 G4 尚未执行。以下结果来自合并后的原工作区，不以各线单测代替整体结果。

## 冻结的基础

- 原分支 `feature/simple-daily-planner`、原 HEAD `f7d8ccf6c7d36f3a7284fa87e082de18df0122b7`，其未提交重构完整保留。
- G0 工作树快照包含 92 个文件，归档 SHA-256 `a058ca9ba1479141559b639e8c28bdd256828413b737502574067f89e82753e6`，清单 SHA-256 `5b63cbbcdc5cb82ecca6c0d1f2047d7603e94a0fa00b31ecb80fc1fe7f6a2e96`。
- G1 共同快照提交 `ff02bb6b8d60fc8faf3f86831e89a21d366d8b07`，包含迁移后的整个工作树、公共合同、共享 fixture 及测试发现配置。它不是旧 HEAD 的别名。
- Node `v26.7.0`、pnpm `10.29.1`、Next `16.3.3`。
- G0 `pnpm check`：164 Vitest + 16 Node API/SQLite，通过；`pnpm build`：API + Web 通过；`pnpm test:e2e`：Chrome/WebKit 共 50 通过（1.3 分钟）。
- G1 公共合同：35 测试通过。各工作线使用相同合同测试；导入历史的原始归档使用向后兼容的可选 `archive` 字段补齐往返保存。

## 并行工作线

工作树位于仓库外的隔离 worktree，每条线仅提交独占文件；共享依赖补丁由集成者同步，不纳入接收方的提交。原始绝对路径不再写入公开文档。

| 工作线 | 分支 | 当前交付 |
| --- | --- | --- |
| A 执行与存储 | `codex/agent-execution-20260908` | `af81778` → `0115434` → `154d0c2` → `d4999a1`：事务、版本、事件、持久账本、采纳/恢复、人工撤销及午夜保护 |
| B 模型运行 | `codex/agent-model-20260908` | `5e24671` → `b3b9ab5` → `4395dc7`：有界运行、严格输出、provider、取消与失效澄清恢复 |
| C 前端 | `codex/agent-frontend-20260908` | `8ec8760` → `b9c9ae4` → `a98ef84`：建议、最终集合预览、原操作恢复、记录与偏好界面 |
| D 上下文历史 | `codex/agent-context-20260908` | `36ff392` → `53ae3e9` → `9e20846`：一致快照、上下文/偏好、历史反馈、独立备份及文档 |
| E 验收 | `codex/agent-evaluation-20260908` | `2841096` → `0e09d32` → `b01f39f` → `c4d9b5a` → `d53b9be`：冻结场景、真实 HTTP/SQLite、跨层故障和浏览器验收 |
| I 集成 | 原工作区；快照 `codex/agent-integration-20260908` | 共享合同、路由配置、HTTP 客户端、时区接线、测试隔离和最终回归 |

## 最终整体验证

最终代码包含上述所有补丁，在原工作区顺序执行以下命令，进程总退出码为 0：

| 命令 | 结果 |
| --- | --- |
| `pnpm check` | ESLint、工作区 TypeScript 全通过；235/235 Vitest；137/137 API/SQLite；23/23 独立 TCP HTTP 验收；无跳过测试 |
| `pnpm build` | API 生产包与 Next.js 生产构建通过 |
| `pnpm test:e2e` | Chrome / Safari WebKit 共 56/56 通过，1.6 分钟；其中 Agent 旅程 6 项，既有清单/重复/迁移/备份流程 50 项 |

原始日志和内容摘要保存在仓库外的受控 `evidence/g2` bundle。其 `integration.json` 记录最终快照 SHA、tree、分线 HEAD、文件列表摘要和日志 SHA-256；集成快照另保存在本地 `codex/agent-integration-20260908` 分支。原 `feature/simple-daily-planner` 分支、HEAD 与暂存区不变，未提交迁移和实现保留在原工作区。未推送或部署。

证据包括真实临时 SQLite 的 COMMIT 前/后子进程退出及重启查询；同键异请求、并发重复投递、事件/回执写入失败的事务回滚；成功 COMMIT 后主动断开 HTTP 应答再查询原操作；手动变更、上下文/偏好、时区、午夜和导入 epoch 的冲突保护；清理详情后原请求不重放；慢模型期间人工任务操作可继续。脚本模型和假 fetch 覆盖限流、超时、畸形输出与晚响应，不代表真实模型行为。

独立复审发现并修复了四类边界：采纳事件不能被误认成删除；同毫秒或次日的事件不能回填为采纳当天结果；失效澄清必须持久结束以释放活动名额；采纳/恢复的最终日期检查、回执和事件必须绑定同一执行时刻。前端另补齐结果未知且原请求未送达时的显式同 ID 重试。首轮浏览器 54/56 的两项失败来自澄清脚本哨兵 fixture，精确修正输入后最终全量 56/56 通过。

日常测试使用临时 SQLite；浏览器测试限 `3100/3002` 和 `newday-e2e-*` 数据目录，不访问开发任务数据库。浏览器日志中已有组件库 PressResponder 警告在 G0 基线同样存在；没有将其误报为本次新增或完全无警告运行。

## 可用范围与后续关卡

- 页面提供当天输入、持久时区/偏好、带来源的建议、最多一轮澄清、最终 1–3 项集合预览、用户采纳/拒绝、反馈、历史和条件恢复。Agent 唯一业务写入为今日重点集合；无动作保留已有重点。
- 网络恢复使用原 request/operation ID。同一标签页刷新可以恢复；关闭标签页后不承诺自动继续，服务端历史仍可查询。任务人工短时 Undo 与 Agent 持久 operation 恢复分别处理。
- Agent 备份 v1、只读导入及历史清理当时通过 API 提供，尚无独立界面。当时任务菜单使用任务备份 v4；当前业务备份已为 v6，详见 README。明细清理不删除最小执行去重账本。
- 默认 `NEWDAY_AGENT_PROVIDER=disabled`；上述 G2 工程验收阶段真实 provider 调用为 **0**。后续按用户要求复用 PaperTrail 配置的真实接入另记于下文。启用配置见 `.env.example` 和 `docs/architecture.md`；G3 批量评测需要固定 provider/模型、可发送数据范围与费用上限。请求超时、输出 token 限制及最多 3 次调用不等于金额预算。
- G3 已冻结 12 个开发场景与 40 个保留场景，尚未运行 40 × 3 = 120 次真实 trial。保留集仓库可见，不声称盲测；自由理由的完整事实支持、自然语言约束理解、建议效用与真实延迟/成本均未验收。
- G4 的人工基线与七天实用记录尚未收集。工程测试通过不能证明模型建议有用或已经满足个人长期使用目标。


## PaperTrail 配置复用与真实接入（2026-09-08）

用户要求使用真实模型，并明确借鉴 PaperTrail 配置。已只读取得其服务地址、模型与凭据，在 NewDay 自己的根 `.env` 配置 `gpt-5.6-sol`，使用现有局域网中转和 `reasoning_effort=none`。NewDay 文件权限为 `0600` 且被 Git 忽略；没有修改 PaperTrail 配置或其本地调用账本。请求限制仍为 30 秒、1200 输出 tokens；本次诊断命令每次最多一次真实出站请求。

真实接入新增三处协议支持：明确许可一个精确 HTTP 来源且禁止跟随重定向；provider 的联合类型投影使用 `anyOf`，本地判别联合合同不变；兼容空 `tool_calls: null`，非空工具调用仍拒绝。对应 B 线提交为 `52536a8`、`e1908b7`、`7ef153f`。

隔离命令 `pnpm agent:smoke` 使用新建临时 SQLite，仅发送三个合成任务及合成当天输入；没有读取开发任务库或 G3 保留集，没有采纳提案。调试过程共 **4 次真实出站请求**，每次保留独立报告：

| 次序 | 真实结果 | 验证判断 |
| --- | --- | --- |
| 1 | HTTP 400，没有提案 | 随后发现并修复 schema 的 `oneOf` 投影；首个错误体未捕获，不把推断写成已证实错误原因 |
| 2 | HTTP 200，但响应包未通过本地解析 | 自动修复请求被该命令的一次出站上限拦住；真实出站 1，运行层尝试次数 2 |
| 3 | HTTP 200、完整 `stop`、187 输出 tokens；响应包解析失败 | 已捕获合成建议用于离线重放；复现并修复 nullable tool_calls 兼容问题 |
| 4 | HTTP 200，真实包中明确观测到 `tool_calls: null`，run `ready` | **真实生成 smoke 通过**；选择两项 must_include 任务，理由引用通过本地校验 |

第 4 次模型返回 ID 为 `gpt-5.6-sol`，运行耗时 **8722 ms**，整个 smoke **9025 ms**，输入 **2614 tokens**、输出 **187 tokens**，只发出一次模型请求且未触发修复。任务、已有重点、重复规则、事件、执行账本及规划版本在生成前后完全相同，执行回执为 0。报告保存在仓库外受控 bundle 的 `live-sol-smoke-4/report.json`，前三次报告同级分别为 `live-sol-smoke-1` 至 `live-sol-smoke-3`。

这次结果证明真实服务能完成一次受约束的合成规划请求；费用仍为未知，没有将 token 用量或失败请求记为零费用。没有启动 G3 的 120 trials，也没有把一次成功当作稳定性、建议效用或七天使用验收。整个实现及配置代码的新快照和回归证据集中保存于 `evidence/live-model/`；不含密钥或开发数据库。


真实接入补丁后的最终回归：`pnpm check` 全通过（235 Vitest、147 API/SQLite、23 独立 HTTP），`pnpm test:e2e` Chrome/WebKit **56/56** 通过（1.6 分钟），`pnpm build` API 与 Web 全通过。中途一次浏览器运行为 52/56，4 项在自动 reset 时遇到测试 API `3002` 的 `ECONNREFUSED`，正文尚未执行；固定源码并重启隔离服务后整套通过，失败日志保留为 `e2e-interrupted.log`。

本地生产服务已按新构建重启：`http://127.0.0.1:3000` 与独立 API `3001` 的健康检查均为 200，两条路径的 Agent 状态均为 `configured=true`、`modelId=gpt-5.6-sol`。现有数据库先备份到 `.test-data/backups/before-live-agent-20260908-092711.sqlite`，再由正常启动从 schema v1 升级到 v2；任务、已有重点和重复规则的行内容及数量与升级前一致。首次使用仍需在页面确认规划时区；没有替用户写入当天目标、采纳建议或发送现有任务到模型。此为本机启用，未推送或远程部署。
