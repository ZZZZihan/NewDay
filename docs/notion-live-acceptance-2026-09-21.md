# NewDay / Notion 隔离工作区真实验收报告（2026-09-21）

本报告记录 COL-39 在独立测试工作区 `NewDay 验收 2026-09-21` 的实际执行。工作区匿名标识为 `notion-20260921-live-01`，工作区 ID 只保留末段 `9bced353`。所有远端读写都限制在该隔离根页面及其子页面；个人工作区未授权、未读取、未写入。令牌、OAuth state/ticket、服务密钥、凭据加密密钥和完整远端 ID 均未写入仓库。

机器可读快照见 [`evidence/notion-live/2026-09-21/summary.json`](../evidence/notion-live/2026-09-21/summary.json)。真实远端执行发生在 `4ae0a5bb2abe565dbf1d7b045d9d51bbca838dbd` 加当时未提交修复上；这段历史不作改写。随后修复与回归验证固定在实现候选 `6685ba45d4bd95cf2e5e0301bf87e0d65b9d14b6`。后续候选没有重新执行整套真实远端矩阵，因此下文分别标明真实执行证据和候选回归证据。

## 固定环境

| 项目 | 本轮值 |
| --- | --- |
| 执行时间 | 2026-09-21 12:47～19:30（Asia/Shanghai） |
| 本机 Web / API | `127.0.0.1:3000` / `127.0.0.1:3001`，独立 SQLite 与凭据库 |
| Worker | 隔离 OAuth Worker 别名 `notion-oauth-worker-live-01`；准确域名和部署标识仅保存在仓库外私有证据中 |
| Notion SDK / API | `@notionhq/client 5.23.0` / `Notion-Version: 2026-03-11` |
| Agent | W3 使用本地确定性契约输出验证陈旧提案栅栏；未把它计作模型质量；H-N01 真实模型小流量试验在固定候选后单独执行 |
| 发布动作 | 真实验收执行时未 push、未 merge、未部署应用；随后仅推送应用候选并建立 Draft PR #19，仍未 merge 或部署应用 |

## 结论

Notion/OAuth 工程验收项 A1～A3、R1～R2、W1～W3、C1～C3、D1 均已在隔离工作区取得真实远端、真实故障代理或独立恢复证据。第二轮独立复审对文档绑定候选给出 P0=0、P1=0、P2=0、P3=1；唯一 P3 是精确竞态场景尚未全部永久进入回归测试。回归覆盖候选 `26ac2219555401b60a9ec5f5e0e86a67bc71cb8a` 已将这些场景固化，并通过完整本机门禁，仍待独立任务复审该精确候选，因此发布结论保持 **NO-GO**。完整 G3 是 40 个保留场景各 3 次共 120 次真实 trial，G4 是固定候选后的连续七天前瞻记录，二者仍按 COL-23/COL-24 独立开放。一次 H-N01 小流量试验不能替代 G3，Notion 同步成功也不能替代 G3/G4。

## 候选与证据绑定

| 证据 | 固定值 | 结论 |
| --- | --- | --- |
| 真实远端执行基线 | `4ae0a5bb2abe565dbf1d7b045d9d51bbca838dbd` 加当时未提交修复 | 证明本报告执行矩阵中的远端观察；不冒充后续候选 SHA |
| 首次候选 | `2f6f0b18be92d5e1d90491e8116afb498fb5e046` | 完整门禁通过；独立复审发现 3 个 P2 和 1 个 P3，因此仅保留为失败历史 |
| 修复实现候选 | `6685ba45d4bd95cf2e5e0301bf87e0d65b9d14b6`，tree `3908e8b47a3dee1ca33ef9d1b958f04f2ef11ee9` | 修复部分初始化重连、凭据竞态、逐项结果展示和公开元数据暴露 |
| 回归覆盖候选 | `26ac2219555401b60a9ec5f5e0e86a67bc71cb8a`，tree `1838916168d515a549c9aafe57c4b56b0871900c` | 固化一个未确认 next step 的只读重连、读取与默认创建中途换凭据，以及 reconnect `needs_review` 双浏览器展示 |
| 相对首次候选的补丁 | SHA-256 `23c53792ccb1ed1cf78b34caff3e400f7876cf389e20e16137651f0d31f6cb64` | 由 `git diff --binary 2f6f0b1..6685ba4` 计算 |
| `pnpm check` | 2026-09-21 20:31（Asia/Shanghai）通过 | Vitest 24/259、API 255、Worker 9、Agent integration 23 |
| `pnpm build` | 2026-09-21 20:31（Asia/Shanghai）通过 | API、Next.js 16.3.3 Web、Worker dry run 均通过；dry run 不等于部署 |
| `pnpm test:e2e` | 2026-09-21 20:33（Asia/Shanghai）通过 | Chrome 与 WebKit 合计 94/94；使用 3100/3002 与一次性 SQLite |

以上最新门禁运行在提交 `26ac221` 的精确源码树上。本报告的后续文档绑定提交只改变报告与去标识摘要；精确 PR head 和最终独立复审结果继续记录在 Draft PR #19 与 COL-39，避免用自引用 SHA 伪造提交内绑定。

## 执行矩阵

| ID | 状态 | 本轮证据 | 未完成或限制 |
| --- | --- | --- | --- |
| A1 | 通过 | 真实取消保持 0 个连接；真实 OAuth 成功；API 重启后连接仍在；刷新后密文改变且 `refresh_pending=false`；断开后旧回调返回 409；新浏览器授权持久化；为隔离根页面补充 connection 访问后，9/9 结构只读核对一致并从 `disconnected` 恢复为 `active`；凭据库为 `0600`；业务备份无凭据标记 | 重新授权只恢复凭据；必须通过只读结构核对后才能恢复连接 |
| A2 | 通过 | 根页面、四张表和四个关系共 9 步逐项确认；最终恰好 9 个 `confirmed` | 无 |
| A3 | 通过 | 根页面、表、relation 三类真实请求均在 Notion 返回 2xx 后丢失下游响应；代理记录 47 个请求，3 个步骤进入 `needs_review`，只读对账后 9/9 `ready`，无重复远端创建或收养 | 证据为仓库外 `0600` JSONL；仓库只保存去标识结论 |
| R1 | 通过 | 真实 Area、Project、同日任务、无日期任务、清空日期任务完成创建/修改/扫描；重启和连续扫描后仍为 3 个 R1 任务，无重复映射 | 无 |
| R2 | 通过 | 真实 `in_trash: true` 页面只归档已核对映射；代理分别注入 403、404、真实响应后移除 schema、真实第一页后中断下一页；水位错误依次为 `permission`、`schema`、`schema`、`network` | 四个场景业务数据哈希、归档数与 outbox 均未变化，失败表 `lastSuccessAt` 未前移 |
| W1 | 通过 | 同一任务真实创建、改标题、改日期、完成、重开；保持一个远端页和稳定 Key；同字段冲突按 Notion 胜出并记录 | 无 |
| W2 | 通过 | 创建响应在真实上游 2xx 后丢失时仅出现 1 次 `POST /v1/pages`，稳定 Key 完整读回后确认；预读断网、429、529 均在页面写入前进入 `preflight_read`，429/529 到期前恢复返回 409 | 每个场景恢复后恰好创建 1 个页面，最终操作均 `confirmed`、连接 `active`、未结操作 0 |
| W3 | 通过 | 本地日期与 Notion 标题不同字段合并；双方日期范围冲突作为完整区间解决；真实修改隔离 Notion 任务并读回后，全表扫描推进规划版本，旧 Agent 提案应用返回 `409 VERSION_CONFLICT` | 今日重点前后哈希一致；提案由本地确定性契约输出产生，仅验证陈旧提案防线，不计作模型质量 |
| C1 | 通过 | 实际写入每日 32、工作日 24、每周一三五 14、月末 1 个页面；受控时钟 `2027-02-01` 生成并真实写入/读回唯一 `2027-02-28` 页 | 二月验证使用受控时钟；远端写入和读回是真实请求 |
| C2 | 修复后通过 | 实例改名、改期至 `2027-03-02`、完成/重开均保持原发生键；规则从 31 改为 30 后既有实例保留；停用后规则 archived、series disabled、2 个既有远端页保留、未结操作 0 | 跨午夜由受控时钟覆盖；另有真实 API 重启 |
| C3 | 通过 | 手动暂停后以受控恢复日 `2026-12-01` 生成 32 个意图，真实写入至 `2027-01-01`；停机区间 `2026-10-23`～`2026-11-30` 为 0；最终 64 个唯一发生键对应 64 个唯一远端页 | 时间推进为受控时钟，避免等待数月；所有页面发送和随后全表扫描为真实请求 |
| D1 | 通过 | 暂停时导出 v6 备份；独立数据库/空凭据库导入后状态 `paused_after_restore`，1 个旧 pending 进入 quarantine；无凭据时只读核对为 `unreadable` 且未伪造授权；独立回退后任务 0，栅栏和隔离记录仍在，原测试库未被覆盖 | 恢复隔离目前没有自动核销入口，符合已记录限制 |

## 真实验收发现并修复的问题

### OAuth Worker 与官方 API 版本

首次真实 token exchange 暴露两个运行环境差异：Worker 的 `fetch` 不接受 `redirect: "error"`，且 token 请求需要与候选固定的 `Notion-Version: 2026-03-11` 一致。Worker 已改为手动重定向模式、显式版本头和不含凭据的诊断日志；真实 claim、ack 和 refresh 随后成功。

### Notion 页面链接域名

真实 API 返回 `https://app.notion.com/...`，原契约只接受 `notion.so`。契约现接受精确的 `app.notion.com`、`notion.so` 及其子域，仍拒绝 HTTP 和相似域名；已增加窄范围合同测试。

### 重新授权后的安全恢复

重新授权只恢复本机凭据，不能自动把已有映射改回可发送状态。`POST .../structure/reconnect` 只读核对已经确认的连续结构前缀；完整 9 项一致时恢复为 `active`，只完成 1～8 项时恢复为可继续初始化的 `paused`，既有未确认尝试仍走原只读对账路径，不会重复创建。权限不足、记录不连续或任一不一致继续保持断开，不调用远端 POST/PATCH。前端展示每一项核对结果和可继续的下一步。

凭据库为每次 claim、refresh、失效和断开分配持久化单调修订号；业务连接只绑定经过核对的修订号。结构核对、全表读取和默认写回传输在远端请求前后均校验该绑定。若另一个进程在核对或同步期间替换凭据，激活提交被拒绝，已发送但结果不确定的写入进入现有 `unknown` 隔离，后续入口保持失败关闭。OAuth claim 或 refresh 完成后还会持久化断开已有业务映射，要求重新只读核对。

第二轮独立复审以临时验收用例实际验证了未确认 next step 的重连、读取中途换凭据和默认创建中途换凭据，均通过。`26ac221` 将三个后端边界永久加入 API 回归，并增加 reconnect `needs_review` 结果在状态刷新后仍逐项可见的 Chrome/WebKit 用例；完整门禁随后以 API 255/255、E2E 94/94 通过。该提交只增加测试，没有重新运行真实 Notion 远端矩阵。

验收期间一次环境定位命令意外把本机凭据加密密钥写入终端输出。该值随即按泄露处理：在保持凭据内容不变的前提下，以新随机 32 字节密钥重新封装凭据库中的密文，更新本机私有环境文件，并用新密钥验证现有凭据可解密。旧密钥不再使用；轮换过程未把 access token、refresh token 或新密钥写入仓库与验收摘要。

### 停用规则后关系字段被隐藏

规则页使用 `in_trash: true` 停用后，真实 Tasks 查询会保留 `Occurrence Key`，但隐藏指向已回收规则的 relation。旧实现把它当成“不完整实例身份”并使整表扫描失败。修复只在以下条件同时满足时沿用旧身份：任务已有经过验证的映射、Occurrence Key 未变、原规则映射已经 archived。活动规则缺少关系时仍失败关闭。修复后真实全表扫描返回 200，规则 archived、series disabled、两个实例及远端页均保留。

## 数量与一致性证据

最终观察到 9 个已确认结构步骤、113 个带远端页的任务映射、113 个唯一远端页、113 个唯一 Client Key、123 个 confirmed outbox、0 个未结 outbox、0 个恢复隔离操作和 2 条冲突记录。新增 5 个映射来自 W2 四个独立写入故障场景与 W3 陈旧提案场景。C3 每日系列为 64 个实例、64 个唯一发生键、64 个唯一远端页；恢复窗口 32 个，停机历史 0 个。

这些计数只证明本轮隔离工作区在验收时的当前状态。它们不证明个人工作区、多用户部署、完整 G3 模型质量或 G4 七天使用效果。

## 备份与凭据隔离

| 文件 | SHA-256 | 权限 | 凭据标记扫描 |
| --- | --- | --- | --- |
| `baseline-before-oauth.json` | `e015331f880b9299b7ca9bc54263a450df58b03febadff8c4ac72465d5498ae0` | `0600` | 0 |
| `business-backup-paused.json` | `02aab1e09ee048dbfadd8c100fb8b0fc1b965cf05b4ec0a256ab6043d7271139` | `0600` | 0 |
| `planner-imported-snapshot.sqlite` | `b5a389aaee88a1b7e9a6293a132d23065b065e1542b22f8f74c683aa80e52027` | `0600` | 不适用 |

标记扫描覆盖 `access_token`、`refresh_token`、`secret_`、`worker_api_key` 和 `credential_key`。本机凭据库单独保存且权限为 `0600`；仓库不保存上述三个文件，只保存去标识摘要。

## 发布门禁

在以下事项完成前保持整体 NO-GO：

1. 由独立任务对修复后的精确候选和本文档绑定提交复审；P0～P2 必须为 0。
2. 只在复审通过的干净候选上重新冻结并执行已授权的 H-N01 小流量真实模型试验：固定 provider、模型、合成数据范围、最多 3 次出站请求、每次最多 1200 输出 token、30 秒超时、未知费用逐命令复核和试验后停止。它只证明单个受控 trial，不形成 G3 结论。
3. 若要形成 G3 结论，另行冻结并完成 40 × 3 全批次、人工评分、用量和费用复核；若出现 provider/model 漂移、意外真实用户数据、业务写入或证据失败立即停止。
4. G4 只能在有效 G3 候选确定后开始连续七天前瞻记录，不能回填或用本轮工程测试替代。

任何后续修复都应在本报告后追加新一轮记录，不能覆盖本轮失败或未执行项。
