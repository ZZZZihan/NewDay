# NewDay / Notion 真实验收与恢复记录（COL-39）

记录日期：2026-09-21。本文是 T8 的执行清单。首轮真实执行、修复、逐项结论和 NO-GO 门禁见[隔离工作区真实验收报告](./notion-live-acceptance-2026-09-21.md)；清单本身不代表已经完成真实验收。所有 Notion 请求须先指定**独立测试工作区**、Public OAuth connection、Worker 地址和允许修改的测试数据范围；令牌、服务密钥、真实任务正文和完整工作区 ID 不写入仓库、PR 或 Linear。

## 当前候选与证据边界

2026-09-21 查询 GitHub 时，PR #1～#15 均为 Open，尚未合并。Notion 主候选按 #3 → #6 → #7 → #8 → #9 → #10 → #11 堆叠；#9 还包含 #5 的同步底座，#14 基于 #11，#15 基于 #14。#12/#13 是分别跟踪 G3/G4 的并行候选。它们不是 `main`、已部署服务或个人数据的当前能力。改变基线、整理堆叠分支或合并后，必须按新 SHA 重新验证。

| 范围 | 当前入口 | 已有证据 | 仍缺少的验收 |
| --- | --- | --- | --- |
| 本地稳定版 M0 | [PR #1](https://github.com/ZZZZihan/NewDay/pull/1)、[PR #2](https://github.com/ZZZZihan/NewDay/pull/2)、[PR #4](https://github.com/ZZZZihan/NewDay/pull/4) | 各 PR/Linear 记录其隔离验证；未合并 | 新合并候选的人工流程、真实数据恢复路径、用户批准合并 |
| 契约 T1 | [PR #3](https://github.com/ZZZZihan/NewDay/pull/3) | 字段、日期、身份、故障契约和合成 fixture | 真实 SDK/工作区行为对照 |
| 授权与结构 T2/T3 | [PR #6](https://github.com/ZZZZihan/NewDay/pull/6)、[PR #7](https://github.com/ZZZZihan/NewDay/pull/7) | 本地 Worker/API 与假网关测试 | Worker 部署、真实授权/轮换、私有根和四表关系读回 |
| 读取、写回 T4～T6 | [PR #8](https://github.com/ZZZZihan/NewDay/pull/8)、[PR #5](https://github.com/ZZZZihan/NewDay/pull/5)、[PR #9](https://github.com/ZZZZihan/NewDay/pull/9) | SQLite、假 Notion 网关/传输、HTTP 与浏览器测试 | 真实分页、限流、回收站、冲突和往返；适用的条件写入能力 |
| 规则与实例 T7 | [PR #10](https://github.com/ZZZZihan/NewDay/pull/10)，`c9b5b2b7bdeac932847b164bf4e76f0fef304aac` | 此 SHA 的 `pnpm check`、`pnpm build`、Chrome/WebKit Notion E2E 10/10 通过；此前 `b1425c6` 的 Notion + 日程 E2E 36/36；月末、改期、停用、重启及 Tasks 中断重试的假网关测试 | 真实规则属性、发生键、实例创建/读回与跨日运行 |
| 暂停与验收手册 T8 | [PR #11](https://github.com/ZZZZihan/NewDay/pull/11)、[PR #14](https://github.com/ZZZZihan/NewDay/pull/14)、[PR #15](https://github.com/ZZZZihan/NewDay/pull/15) | 持久暂停入口、429/529 写前预读截止时间、单步结构核对、恢复隔离明细与远端只读核对；2026-09-21 首轮真实 OAuth、读写、规则和恢复记录见[执行报告](./notion-live-acceptance-2026-09-21.md) | A1/A3/R2/W2/W3 剩余子项、最终候选门禁与用户认可；当前结论 NO-GO |
| G3 / G4 | [COL-23](https://linear.app/colife/issue/COL-23)、[COL-24](https://linear.app/colife/issue/COL-24)；[PR #12](https://github.com/ZZZZihan/NewDay/pull/12)、[PR #13](https://github.com/ZZZZihan/NewDay/pull/13) | G3 零真实调用预检和 G4 前瞻记录协议 | 冻结预算与真实模型评测；人工基线和连续七天原始记录 |

`pnpm build` 中 Worker 的 `wrangler deploy --dry-run` 只验证打包，不是部署。COL-39 已执行一轮真实 OAuth、Notion 读写和独立恢复演练，但故障注入矩阵及部分收尾仍未完成；具体通过项和缺口以[执行报告](./notion-live-acceptance-2026-09-21.md)为准，缺失证据继续记为未执行。

## 启动真实隔离测试前

1. 固定待测提交 SHA、PR 依赖、Node/pnpm 版本、Notion SDK 版本 `5.23.0`、`Notion-Version: 2026-03-11` 和测试时间段。先在独立工作树运行 `pnpm check`、`pnpm build` 及相关 Chrome/WebKit E2E，记录退出结果。待测版本变化后标记旧运行记录失效。
2. 由用户指定与个人正式数据分离的 Notion 工作区，并确认允许建立 `NewDay (<安装标记>)` 私有根、四张测试表、测试任务和规则；确认 Worker 的 Cloudflare 项目及回调 HTTPS origin。正式个人工作区接入须在测试工作区通过后再次得到用户确认。
3. 按 [OAuth 配置说明](./notion-oauth-operations.md)分别设置 Worker 的 `NOTION_CLIENT_ID`、`NOTION_CLIENT_SECRET`、`NOTION_REDIRECT_URI`、`LOCAL_RETURN_ORIGIN`、`LOCAL_API_KEY`，以及本机的 `NEWDAY_NOTION_WORKER_ORIGIN`、`NEWDAY_NOTION_WORKER_API_KEY`、`NEWDAY_NOTION_CREDENTIAL_KEY`。服务密钥与凭据加密密钥必须是不同的 32 字节值。不要把环境文件、令牌或密钥上传。
4. 使用独立 `NEWDAY_DATABASE_PATH` 和 `NEWDAY_NOTION_CREDENTIAL_PATH`。在授权前通过界面导出当前业务 JSON 备份并保留原件；测试环境也保留测试前备份。确认本机 Web/API 回环地址与 Worker 的 `LOCAL_RETURN_ORIGIN` 完全一致。默认的 `data/newday.sqlite` 与个人凭据库不作为试验目标。
5. 在 Notion 连接页确认工作区身份、连接状态和结构状态。任何 `needs_review`、`unknown`、`quarantined`、`refresh_pending`、`paused_after_restore` 或不匹配的工作区 ID 都先进入下方恢复流程，不继续写入。

## 真实工作区执行矩阵

每项都要记：候选 SHA、匿名化工作区标识、开始/结束时间、操作人、NewDay 本地任务 ID、远端页面 ID 的脱敏末段、实际前后值、连接/扫描/操作状态、结果与证据位置。A3/R2/W2 的故障部分需要可控代理或等效注入器；逐项记录注入层、触发请求和时间、上游是否实际收到请求、上游响应以及本机看到的错误。假网关测试只算离线证据；未建立真实工作区的可复核注入方法时，该项记为“未执行”，不得靠碰巧断网或页面快照标记通过。失败或未运行保留原状态。

| ID | 隔离工作区操作 | 通过条件 |
| --- | --- | --- |
| A1 | 授权、取消一次、重新授权、重启 API、刷新令牌、断开再授权 | 票据不可重放；浏览器和业务备份不含令牌；刷新只用同一次尝试；断开不被旧回调反向恢复 |
| A2 | 确认建立私有根、Areas、Projects、Tasks、Rules 及四个关系，逐个读回 ID/类型/目标 | 恰好一套结构，九个初始化步骤确认；`database_id` 与 `data_source_id` 不混用 |
| A3 | 在隔离工作区对根、表和关系各注入一次上游已收到请求但本机丢失响应，再重启并点“重新核对” | 当前步骤只读核对且每次点击只执行一步；不盲目发送第二次 POST/PATCH；零个或多个候选时保留 `needs_review` |
| R1 | 创建并修改主线、项目与一次性任务；测试同日、跨日区间、无日期和清空日期 | 归属及链接正确；日期按包含首尾显示；无日期不进入每日清单；重复扫描/重启无副本 |
| R2 | 在隔离工作区撤销测试连接权限，并通过可控代理分别注入 404、部分分页和 schema 错误；另用测试页验证明确 `in_trash: true` | 不完整时相应表水位不推进、不批量归档；只有可核对的回收站证据触发归档 |
| W1 | NewDay 创建一次性任务、改标题/日期、完成/重开；Notion 再改同字段 | 单个远端页面和稳定 `NewDay Key`；两端读回一致；同字段冲突显示共同基准、两端值和决议 |
| W2 | 在隔离工作区分别注入创建响应丢失、写前预读断网、写前预读 429/529 及 `Retry-After`，恢复后核对 | 未确认写入不盲目重发；写前预读失败保留 `pending` 并暂停；429/529 的截止时间在状态中可见且到期前不能恢复，之后由人明确恢复发送 |
| W3 | 本地改日期、Notion 改标题；两端同时改日期区间；建议生成后远端改期 | 不同字段合并；日期整体解决冲突；旧 Agent 提案拒绝采纳，重点不被过期建议覆盖 |
| C1 | 新建每日、工作日、每周和 31 日月度规则；检查用户时区今天至今天＋31 天 | 有且只有稳定 `Occurrence Key` 的窗口实例；2 月夹到月末；本地旧规则不被迁移 |
| C2 | 改期、改名、完成/重开一个实例；修改/停用规则；重启并跨午夜扫描 | 实例 ID/发生键不变；已完成历史和已生成未来页不被规则刷新覆盖；新规则在已生成边界后生效；停用不删除既有页 |
| C3 | 停机超过生成窗口，再恢复和备份导入 | 已有映射不复制；只从恢复当天生成新窗口，不补造从未生成的停机历史；恢复后的发送保持隔离直到人工核对 |
| D1 | 暂停、导出 JSON、在另一独立数据库导入、核对，再执行一次受控回退演练 | 连接在恢复后暂停，旧 outbox 隔离；没有令牌的备份不假称能自动恢复授权；回退演练不覆盖原测试库 |

正常网络、授权有效、API 持续运行、固定测试规模且无持续限流时，R1/W1/C1 的远端修改应在 **5 分钟内**反映；保存每次操作与可见结果的时间戳。异常恢复另记时长，不混作正常延迟。

## 暂停与恢复手册

- **先停写。** 在“Notion 连接”对目标工作区点“暂停同步”，等状态显示“已手动暂停”，再记录各表扫描水位、outbox 操作 ID 并导出业务 JSON。此持久栅栏阻止启动新一轮自动读取和新的页面发送；已经开始的扫描可继续分页直到本轮退出，已经发出的写请求也可能完成。若暂停时存在 `sending`，等待读回到 `confirmed`/`pending`，或按未知结果核对；停止 API 进程可能留下未知结果，不得把它当作未发送。仅当 API 无法响应或无法建立暂停栅栏时，才先停止进程并把所有 `sending` 记为待核对。
- **授权故障。** `refresh_pending` 只重试原轮换尝试；`reauthorization_required` 重新授权并核对同一工作区。断开只删除本机凭据，不等于在 Notion 撤销连接。凭据密钥丢失时不能从业务备份推算令牌，须保留旧库并重新授权。
- **结构未知。** `needs_review` 时点“重新核对”仅执行当前已有尝试的一次只读核对；服务端检查步骤和尝试标识，另一标签页已经推进时返回 409，须刷新后重新判断。若该步骤转为已确认，后续步骤须另行点“建立或继续结构”才可能发 POST/PATCH。核对根标题标记、父页面、database/data source 和关系目标。搜索零结果可能是索引延迟；不清除创建尝试记录，也不自动重发当前结构请求。手动同步暂停针对已就绪工作区，不能替代结构初始化期间的人工停步。
- **发送未确认。** `pending` 可在确认工作区和字段后等待队列或手动发送；`paused` 且原因为 `manual` 或 `preflight_read`，在已发请求结算、远端恢复并核对后点“恢复发送”。429/529 写前预读会显示并记录 `Retry-After` 截止时间；到期前即使点击恢复，API 也会拒绝并提示截止时间。到期后仍须人工点击，队列不会自动重试。`unknown`/`paused_unknown` 只用“只读核对”检查原操作和稳定键；多页、查询不完整或结果仍未知时继续暂停。`quarantined`/`paused_after_restore` 当前无一键核销流程。连接面板和同步状态接口会列出恢复前本机未结算的操作，以及导入备份中的未完成操作；即使当前 outbox 为空，也须保留这些操作 ID、原状态和远端标识逐项核对，不越过隔离发送新意图。在连接页对每项隔离操作点“只读核对隔离操作”，API 用原数据集 epoch 和操作 ID 定位隔离记录，按原映射读页面或完整搜索稳定键，并保存本次观察时间、匹配结果、远端页面和字段值。`not_observed` 只说明此次未看到页面，不证明旧写请求未成功；查询不完整、多页、身份不符、回收站或读取失败继续隔离。即便观察值与原意图一致，也只是一时的读回记录，不能自动核销或开放发送。核对期间再次替换数据集或改变连接身份时，本次记录被拒绝，须刷新重做。
- **扫描失败。** 检查失败类别、权限、字段类型和分页；不因查询缺项或 404 删除本地任务。仅确认页面 `in_trash: true` 后允许相应归档。修复远端问题后重新全量扫描，核对上次成功时间和任务数量。
- **备份与回退。** 业务 JSON v6 包含规则/任务映射和 outbox 元数据，不含 OAuth 凭据。导入先设置发送栅栏并换 `datasetEpoch`，未完成操作进入隔离。先在新独立 SQLite 中验证导入、工作区和远端当前值，再决定是否用于原环境；不能仅恢复本地文件就覆盖可能较晚到达的远端请求。SQLite schema v7 是向前迁移，旧二进制无法直接打开 v7 库；代码回退须配合已验证的旧版本备份，在独立环境先演练。

更细的状态语义见[一次性任务写回说明](./notion-write-operations.md)、[结构初始化说明](./notion-structure-operations.md)与[联动契约](./notion-sync-contract.md)。恢复时不得把“API 返回成功”、“测试通过”或“页面看起来正确”单独当成远端写入与历史完整性的证明。

## 已知限制与交付门禁

- 当前未证明 Notion 对此接口提供适用的条件写入；写前读、按字段 PATCH、写后读只支持可检测范围内的冲突处理与最终收敛，不能承诺任意并发下零覆盖。恢复隔离操作仍需人工核对。写前预读遇到 429/529 采用持久截止时间和人工恢复，真实响应的 `Retry-After` 解析仍须验证；写请求结果不明不会自动重发。
- 同步只在本机 API 运行且连接可用时进行；Worker 仅负责授权和令牌。Notion 规则修改后，已生成的实例固定为例外，新规则从最晚已生成原始发生日之后生效；停机期间从未生成的历史不补造。删除联动任务及修改归属仍在 Notion 完成。
- COL-39 完成条件是 A/R/W/C/D 记录与可复核证据、固定版本、已知限制、恢复演练及用户认可；真实个人工作区接入另需用户确认。任何未通过项写入 Linear 阻塞，不将 Draft PR、假网关测试、Worker dry run 或软件构建视为真实验收。
- G3 与 G4 分属 COL-23/COL-24：真实模型请求须先冻结 provider/模型、提示词/schema、数据范围、费用预算和停止条件；G4 需要人工基线与连续七天原始记录。同步通过不能代替这两项验收。

## 运行记录模板

| 字段 | 本轮填写 |
| --- | --- |
| 候选 SHA / PR / 测试工作区匿名标识 | 未填写 |
| Worker 部署版本与回调 origin（无凭据） | 未填写 |
| 本机 API/Web 版本、独立数据库路径的匿名标识 | 未填写 |
| A1～D1 各项开始/结束时间、结果、证据位置 | 未执行 |
| 失败类别、操作 ID、恢复动作与复核结果 | 未记录 |
| 备份文件校验值、导入演练与回退结果 | 未执行 |
| 已知限制、用户验收结论与日期 | 未确认 |

每轮测试单独复制此表到 COL-39，并保留原始记录。不能在后续修复后覆盖先前失败；若候选版本变化，分段记录受影响场景。
